# Sidan behöver inga miljövariabler för att byggas eller köras.
# All media hämtas hem automatiskt av `npm run build` och serveras
# från egen domän.

# Behövs bara när du kör `npm run fetch-reviews`
# GOOGLE_MAPS_API_KEY=
